diff --git a/src/controllers/costsController.js b/src/controllers/costsController.js
index 8279ea2..d9aa48e 100644
--- a/src/controllers/costsController.js
+++ b/src/controllers/costsController.js
@@ -2035,145 +2035,84 @@ const exportSelectedMaterialsExcel = async (req, res) => {
 
         const workbook = new Excel.Workbook();
         const worksheet = workbook.addWorksheet('مواد محددة');
-        worksheet.properties.outlineLevelRow = 1;
-
-        const parseJsonValue = (value) => {
-            if (value === null || value === undefined || value === '') return null;
-            try {
-                if (typeof value === 'string') return JSON.parse(value);
-                return value;
-            } catch (_) {
-                return null;
-            }
-        };
-
-        const summarizeJson = (value) => {
-            const parsed = parseJsonValue(value);
-            if (parsed === null) return '';
-            if (Array.isArray(parsed)) return `JSON Array (${parsed.length}) - تفاصيل في الصف المطوي`;
-            if (typeof parsed === 'object') return `JSON Object (${Object.keys(parsed).length}) - تفاصيل في الصف المطوي`;
-            return `JSON Value - تفاصيل في الصف المطوي`;
-        };
-
-        const stringifyPretty = (value) => {
-            const parsed = parseJsonValue(value);
-            if (parsed === null) return '';
-            return JSON.stringify(parsed, null, 2);
-        };
+        worksheet.views = [{ rightToLeft: true }];
 
         const columns = [
-            { header: 'ID', key: 'id', width: 10 },
-            { header: 'نوع المادة', key: 'material_type', width: 20 },
-            { header: 'اسم المادة', key: 'material_name', width: 35 },
-            { header: 'مصدر المادة', key: 'material_origin', width: 14 },
-            { header: 'طريقة الحساب', key: 'calculation_method', width: 16 },
-            { header: 'السعر قبل الهدر', key: 'price_before_waste', width: 18 },
-            { header: 'السعر قبل الهدر (ل.س)', key: 'price_before_waste_syp', width: 20 },
-            { header: 'الوزن الإجمالي', key: 'gross_weight', width: 14 },
-            { header: 'نسبة الهدر', key: 'waste_percentage', width: 12 },
+            { header: '#', key: 'row_number', width: 8 },
+            { header: 'اسم المادة', key: 'material_name', width: 34 },
             { header: 'وحدة التعبئة', key: 'packaging_unit', width: 16 },
-            { header: 'الوزن الصافي', key: 'packaging_weight', width: 14 },
-            { header: 'وزن وحدة التعبئة', key: 'packaging_unit_weight', width: 18 },
-            { header: 'ثمن العبوة الفارغة', key: 'empty_package_price', width: 18 },
-            { header: 'ثمن العبوة الفارغة (ل.س)', key: 'empty_package_price_syp', width: 22 },
-            { header: 'ثمن اللصاقة', key: 'sticker_price', width: 14 },
-            { header: 'ثمن اللصاقة (ل.س)', key: 'sticker_price_syp', width: 18 },
-            { header: 'مصاريف إضافية', key: 'additional_expenses', width: 16 },
-            { header: 'مصاريف إضافية (ل.س)', key: 'additional_expenses_syp', width: 20 },
-            { header: 'مصروف عمال', key: 'labor_cost', width: 14 },
-            { header: 'مصروف عمال (ل.س)', key: 'labor_cost_syp', width: 18 },
-            { header: 'مواد حافظة', key: 'preservatives_cost', width: 14 },
-            { header: 'مواد حافظة (ل.س)', key: 'preservatives_cost_syp', width: 18 },
-            { header: 'ثمن الكرتون', key: 'carton_price', width: 14 },
-            { header: 'ثمن الكرتون (ل.س)', key: 'carton_price_syp', width: 18 },
-            { header: 'شد الكرتون', key: 'pieces_per_package', width: 12 },
-            { header: 'ثمن الطبلية', key: 'pallet_price', width: 14 },
-            { header: 'ثمن الطبلية (ل.س)', key: 'pallet_price_syp', width: 18 },
-            { header: 'عدد الطرود في الطبلية', key: 'packages_per_pallet', width: 20 },
             { header: 'كلفة القطعة', key: 'unit_cost', width: 14 },
-            { header: 'كلفة القطعة (ل.س)', key: 'unit_cost_syp', width: 18 },
             { header: 'كلفة الطرد', key: 'package_cost', width: 14 },
-            { header: 'كلفة الطرد (ل.س)', key: 'package_cost_syp', width: 18 },
-            { header: 'أوزان إضافية (JSON)', key: 'extra_weights', width: 34 },
-            { header: 'وزن الطرد القائم', key: 'gross_package_weight', width: 18 },
-            { header: 'بنود مصاريف إضافية (JSON)', key: 'additional_expense_items', width: 34 },
-            { header: 'ملاحظات خارجية', key: 'external_notes', width: 26 },
-            { header: 'القيم الخام (JSON)', key: 'numeric_raw', width: 34 },
-            { header: 'تاريخ الإضافة', key: 'created_at', width: 20 },
-            { header: 'آخر تحديث', key: 'updated_at', width: 20 },
-            { header: 'تاريخ الحذف', key: 'deleted_at', width: 20 }
+            { header: 'وزن الطرد القائم', key: 'gross_package_weight', width: 16 },
+            { header: 'الوزن الصافي', key: 'packaging_weight', width: 14 },
+            { header: 'شد الكرتون', key: 'pieces_per_package', width: 12 },
+            { header: 'كلفة الكيلو', key: 'cost_per_kg', width: 14 },
+            { header: 'تاريخ الإضافة', key: 'created_at', width: 14 }
         ];
-
         worksheet.columns = columns;
-        worksheet.views = [{ rightToLeft: true }];
 
-        materials.forEach((m) => {
-            const row = applyMaterialRaw(m);
-            worksheet.addRow({
-                id: row.id,
-                material_type: row.material_type,
-                material_name: row.material_name,
-                material_origin: row.material_origin || 'internal',
-                calculation_method: row.calculation_method,
-                price_before_waste: row.price_before_waste,
-                price_before_waste_syp: row.price_before_waste_syp,
-                gross_weight: row.gross_weight,
-                waste_percentage: row.waste_percentage,
-                packaging_unit: row.packaging_unit,
-                packaging_weight: row.packaging_weight,
-                packaging_unit_weight: row.packaging_unit_weight,
-                empty_package_price: row.empty_package_price,
-                empty_package_price_syp: row.empty_package_price_syp,
-                sticker_price: row.sticker_price,
-                sticker_price_syp: row.sticker_price_syp,
-                additional_expenses: row.additional_expenses,
-                additional_expenses_syp: row.additional_expenses_syp,
-                labor_cost: row.labor_cost,
-                labor_cost_syp: row.labor_cost_syp,
-                preservatives_cost: row.preservatives_cost,
-                preservatives_cost_syp: row.preservatives_cost_syp,
-                carton_price: row.carton_price,
-                carton_price_syp: row.carton_price_syp,
-                pieces_per_package: row.pieces_per_package,
-                pallet_price: row.pallet_price,
-                pallet_price_syp: row.pallet_price_syp,
-                packages_per_pallet: row.packages_per_pallet,
-                unit_cost: row.unit_cost,
-                unit_cost_syp: row.unit_cost_syp,
-                package_cost: row.package_cost,
-                package_cost_syp: row.package_cost_syp,
-                extra_weights: summarizeJson(row.extra_weights),
-                gross_package_weight: row.gross_package_weight,
-                additional_expense_items: summarizeJson(row.additional_expense_items),
-                external_notes: row.external_notes || '',
-                numeric_raw: summarizeJson(row.numeric_raw),
-                created_at: row.created_at,
-                updated_at: row.updated_at,
-                deleted_at: row.deleted_at || ''
-            });
+        const formatDate = (dateValue) => {
+            if (!dateValue) return '-';
+            const d = new Date(dateValue);
+            if (Number.isNaN(d.getTime())) return '-';
+            const day = String(d.getDate()).padStart(2, '0');
+            const month = String(d.getMonth() + 1).padStart(2, '0');
+            const year = d.getFullYear();
+            return `${day}/${month}/${year}`;
+        };
 
-            const jsonDetails = [
-                { label: 'تفاصيل extra_weights', value: stringifyPretty(row.extra_weights) },
-                { label: 'تفاصيل additional_expense_items', value: stringifyPretty(row.additional_expense_items) },
-                { label: 'تفاصيل numeric_raw', value: stringifyPretty(row.numeric_raw) }
-            ].filter((x) => x.value);
-
-            jsonDetails.forEach((detail) => {
-                const detailRow = worksheet.addRow({
-                    id: '',
-                    material_type: '',
-                    material_name: `↳ ${row.material_name || ''}`,
-                    material_origin: detail.label,
-                    calculation_method: detail.value
-                });
-                detailRow.outlineLevel = 1;
-                detailRow.hidden = true;
-                detailRow.getCell('calculation_method').alignment = { wrapText: true, vertical: 'top' };
+        const formatNum = (value, decimals = 2) => {
+            if (value === null || value === undefined || value === '') return '-';
+            if (typeof value === 'string' && /^-?\d+(\.\d+)?$/.test(value.trim())) {
+                return value.trim();
+            }
+            const n = typeof value === 'number' ? value : parseFloat(value);
+            if (!Number.isFinite(n)) return '-';
+            if (Number.isInteger(n)) return String(n);
+            const fixed = n.toFixed(Math.max(0, decimals));
+            return fixed.replace(/\.?0+$/, '');
+        };
+
+        const currencySymbol = (req.defaultCurrency && req.defaultCurrency.symbol) ? req.defaultCurrency.symbol : '$';
+        const isSyp = req.defaultCurrency && req.defaultCurrency.code === 'SYP';
+
+        materials.forEach((material, index) => {
+            const row = applyMaterialRaw(material);
+            const unitCostValue = isSyp ? (row.unit_cost_syp || row.unit_cost) : row.unit_cost;
+            const packageCostValue = isSyp ? (row.package_cost_syp || row.package_cost) : row.package_cost;
+            const netWeightNum = parseFloat(row.packaging_weight);
+            const unitCostNum = parseFloat(unitCostValue);
+            const computedCostPerKg = (Number.isFinite(netWeightNum) && netWeightNum > 0 && Number.isFinite(unitCostNum))
+                ? (unitCostNum / netWeightNum)
+                : null;
+            const storedCostPerKg = isSyp
+                ? (row.price_before_waste_syp ?? row.price_before_waste)
+                : row.price_before_waste;
+            const costPerKgValue = (storedCostPerKg !== null && storedCostPerKg !== undefined && String(storedCostPerKg).trim() !== '')
+                ? storedCostPerKg
+                : computedCostPerKg;
+
+            worksheet.addRow({
+                row_number: index + 1,
+                material_name: row.material_name || '-',
+                packaging_unit: row.packaging_unit || '-',
+                unit_cost: `${formatNum(unitCostValue, isSyp ? 0 : 2)} ${currencySymbol}`.trim(),
+                package_cost: `${formatNum(packageCostValue, isSyp ? 0 : 2)} ${currencySymbol}`.trim(),
+                gross_package_weight: `${formatNum(row.gross_package_weight, 3)} كجم`,
+                packaging_weight: `${formatNum(row.packaging_weight, 3)} كجم`,
+                pieces_per_package: formatNum(row.pieces_per_package, 0),
+                cost_per_kg: `${(costPerKgValue === null ? '-' : formatNum(costPerKgValue, isSyp ? 0 : 2))} ${currencySymbol}`.trim(),
+                created_at: formatDate(row.created_at)
             });
         });
 
         worksheet.getRow(1).font = { bold: true };
-        worksheet.getRow(1).alignment = { horizontal: 'center' };
+        worksheet.getRow(1).alignment = { horizontal: 'center', vertical: 'middle' };
+        worksheet.eachRow((row, rowNumber) => {
+            if (rowNumber === 1) return;
+            row.alignment = { vertical: 'middle', horizontal: 'center' };
+            row.getCell('material_name').alignment = { vertical: 'middle', horizontal: 'right' };
+        });
 
         const fileName = `${uuidv4()}.xlsx`;
         const savePath = path.join(__dirname, '../public/materials_list_excel', fileName);
diff --git a/src/views/costs/cost-statement.ejs b/src/views/costs/cost-statement.ejs
index 87b77b9..dcc0164 100644
--- a/src/views/costs/cost-statement.ejs
+++ b/src/views/costs/cost-statement.ejs
@@ -1,11 +1,15 @@
 ﻿<%
 function formatNumberDisplay(value) {
-    if (value === null || value === undefined || value === '') return '-';
-    if (typeof value === 'string' && /^-?\d+(\.\d+)?$/.test(value.trim())) return value.trim();
-    const num = parseFloat(value);
-    if (isNaN(num)) return '-';
-    if (Number.isInteger(num)) return String(num);
-    return num.toString();
+  if (value === null || value === undefined || value === '') return '-';
+  if (typeof value === 'string' && /^-?\d+(\.\d+)?$/.test(value.trim())) return value.trim();
+  const num = (typeof value === 'number') ? value : parseFloat(value);
+  if (!Number.isFinite(num)) return '-';
+  if (num === 0 || Object.is(num, -0)) return '0';
+  const safe = Number(num.toFixed(12));
+  let out = Number.isInteger(safe) ? String(safe) : safe.toString();
+  out = out.replace(/(\.\d*?[1-9])0+$/,'$1').replace(/\.0+$/,'');
+  if (out === '-0') out = '0';
+  return safe < 0 ? ('\u200E' + out) : out;
 }
 %>
 <div class="container mt-4">
@@ -15,6 +19,9 @@ function formatNumberDisplay(value) {
                 <h2 class="mb-0"><i class="bi bi-calculator"></i> بيان الكلفة</h2>
 
                 <div class="d-flex flex-wrap gap-2 align-items-center cost-actions-bar">
+                    <a href="/costs" class="btn btn-outline-secondary">
+                        <i class="bi bi-arrow-right me-1"></i> العودة للتكاليف
+                    </a>
                     <div class="input-group cost-search-group">
                         <span class="input-group-text"><i class="bi bi-search"></i></span>
                         <input type="text" id="materialKeywordSearch" class="form-control" placeholder="ابحث بالاسم أو النوع">
@@ -35,9 +42,6 @@ function formatNumberDisplay(value) {
                     <a href="/costs/cost-statement/deleted" class="btn btn-outline-danger">
                         <i class="bi bi-trash3"></i> سلة المحذوفات
                     </a>
-                    <a href="/costs" class="btn btn-outline-secondary">
-                        <i class="bi bi-arrow-right me-1"></i> العودة للتكاليف
-                    </a>
                 </div>
             </div>
 
@@ -114,12 +118,17 @@ function formatNumberDisplay(value) {
                                         <% 
                                             const netWForCalc = parseFloat(material.packaging_weight);
                                             const unitCostVal = parseFloat(material.unit_cost) || 0;
-                                            const costPerKg = (!isNaN(netWForCalc) && netWForCalc > 0) ? (unitCostVal / netWForCalc) : null;
+                                            const computedCostPerKg = (!isNaN(netWForCalc) && netWForCalc > 0) ? (unitCostVal / netWForCalc) : null;
+                                            const storedCostSource = (defaultCurrency && defaultCurrency.code === 'SYP')
+                                                ? material.price_before_waste_syp
+                                                : material.price_before_waste;
+                                            const storedCostPerKg = (storedCostSource !== null && storedCostSource !== undefined && String(storedCostSource).trim() !== '') ? storedCostSource : null;
+                                            const costPerKg = storedCostPerKg !== null ? storedCostPerKg : computedCostPerKg;
                                         %>
                                         <%= (costPerKg != null)
                                             ? ((defaultCurrency && defaultCurrency.code === 'SYP')
-                                                ? String(Math.round(costPerKg))
-                                                : formatNumberDisplay(Math.round(costPerKg * 100) / 100))
+                                                ? formatNumberDisplay(costPerKg)
+                                                : formatNumberDisplay(costPerKg))
                                             : '-' 
                                         %> <%= defaultCurrency ? defaultCurrency.symbol : '$' %>
                                     </td>
@@ -2761,9 +2770,14 @@ function formatDate(dateString) {
 function rawNumDisplay(value) {
     if (value === null || value === undefined || value === '') return '0';
     if (typeof value === 'string' && /^-?\d+(\.\d+)?$/.test(value.trim())) return value.trim();
-    const n = parseFloat(value);
-    if (isNaN(n)) return '0';
-    return Number.isInteger(n) ? String(n) : n.toString();
+    const n = (typeof value === 'number') ? value : parseFloat(value);
+    if (!Number.isFinite(n)) return '0';
+    if (n === 0 || Object.is(n, -0)) return '0';
+    const safe = Number(n.toFixed(12));
+    let out = Number.isInteger(safe) ? String(safe) : safe.toString();
+    out = out.replace(/(\.\d*?[1-9])0+$/,'$1').replace(/\.0+$/,'');
+    if (out === '-0') out = '0';
+    return safe < 0 ? ('\u200E' + out) : out;
 }
 function formatAggregateDisplay(value) {
     const n = parseFloat(value);
@@ -3517,3 +3531,5 @@ document.addEventListener('DOMContentLoaded', function () {
 });
 </script>
 
+
+
diff --git a/src/views/costs/material-preview.ejs b/src/views/costs/material-preview.ejs
index f3bd165..de1cc55 100644
--- a/src/views/costs/material-preview.ejs
+++ b/src/views/costs/material-preview.ejs
@@ -1,4 +1,4 @@
-<%- include('../layouts/header') %>
+﻿<%- include('../layouts/header') %>
 <%
 const __isSyp = (typeof defaultCurrency !== 'undefined' && defaultCurrency && defaultCurrency.code === 'SYP');
 const __exchangeRate = (typeof exchangeRate !== 'undefined' && exchangeRate) ? parseFloat(exchangeRate) : 13000;
@@ -6,10 +6,14 @@ const __currencySymbol = (typeof defaultCurrency !== 'undefined' && defaultCurre
 function formatNumberDisplay(value) {
   if (value === null || value === undefined || value === '') return '-';
   if (typeof value === 'string' && /^-?\d+(\.\d+)?$/.test(value.trim())) return value.trim();
-  const num = parseFloat(value);
-  if (isNaN(num)) return '-';
-  if (Number.isInteger(num)) return String(num);
-  return num.toString();
+  const num = (typeof value === 'number') ? value : parseFloat(value);
+  if (!Number.isFinite(num)) return '-';
+  if (num === 0 || Object.is(num, -0)) return '0';
+  const safe = Number(num.toFixed(12));
+  let out = Number.isInteger(safe) ? String(safe) : safe.toString();
+  out = out.replace(/(\.\d*?[1-9])0+$/,'$1').replace(/\.0+$/,'');
+  if (out === '-0') out = '0';
+  return safe < 0 ? ('\u200E' + out) : out;
 }
 function formatTotalDisplay(value) {
   const num = parseFloat(value);
@@ -276,7 +280,7 @@ const __isExternalMaterial = material.material_origin === 'external';
                                 </tr>
                                 <tr>
                                     <td><strong>الوزن الصافي:</strong></td>
-                                    <td><%= (parseFloat(material.gross_weight) * (1 - parseFloat(material.waste_percentage) / 100)).toFixed(2) %> كجم</td>
+                                    <td><%= formatNumberDisplay(parseFloat(material.gross_weight) * (1 - parseFloat(material.waste_percentage) / 100)) %> كجم</td>
                                 </tr>
                             </table>
                         </div>
@@ -577,7 +581,7 @@ function printMaterialDetails() {
                     <tr><th>السعر قبل الهدر</th><td><%= formatNumberDisplay(material.price_before_waste) %> <%= defaultCurrency ? defaultCurrency.symbol : '$' %>/كجم</td></tr>
                     <tr><th>الوزن الجمالي</th><td><%= formatNumberDisplay(material.gross_weight) %> كجم</td></tr>
                     <tr><th>نسبة الهدر</th><td><%= formatNumberDisplay(material.waste_percentage) %>%</td></tr>
-                    <tr><th>الوزن الصافي</th><td><%= (parseFloat(material.gross_weight) * (1 - parseFloat(material.waste_percentage) / 100)).toFixed(2) %> كجم</td></tr>
+                    <tr><th>الوزن الصافي</th><td><%= formatNumberDisplay(parseFloat(material.gross_weight) * (1 - parseFloat(material.waste_percentage) / 100)) %> كجم</td></tr>
                 </table>
             </div>
             
@@ -710,3 +714,5 @@ document.addEventListener('DOMContentLoaded', function(){
   }
 });
 </script>
+
+
diff --git a/src/views/costs/materials-print-list.ejs b/src/views/costs/materials-print-list.ejs
index 430ee4f..c58b961 100644
--- a/src/views/costs/materials-print-list.ejs
+++ b/src/views/costs/materials-print-list.ejs
@@ -1,16 +1,21 @@
 <% function fmtDate(d){ if(!d) return '-'; const dt=new Date(d); const dd=String(dt.getDate()).padStart(2,'0'); const mm=String(dt.getMonth()+1).padStart(2,'0'); const yy=dt.getFullYear(); return `${dd}/${mm}/${yy}`; }
 function num(v, d=2){
   if (typeof v === 'string' && /^-?\d+(\.\d+)?$/.test(v.trim())) return v.trim();
-  const n=parseFloat(v);
-  if(isNaN(n)) return '-';
-  return Number.isInteger(n)? String(n): n.toString();
+  const n = (typeof v === 'number') ? v : parseFloat(v);
+  if(!Number.isFinite(n)) return '-';
+  if (n === 0 || Object.is(n, -0)) return '0';
+  const safe = Number(n.toFixed(12));
+  let out = Number.isInteger(safe) ? String(safe) : safe.toString();
+  out = out.replace(/(\.\d*?[1-9])0+$/,'$1').replace(/\.0+$/,'');
+  if (out === '-0') out = '0';
+  return safe < 0 ? ('\u200E' + out) : out;
 }
 const currencySymbol = (typeof defaultCurrency !== 'undefined' && defaultCurrency && defaultCurrency.symbol) ? defaultCurrency.symbol : '';
 %>
 <div class="container-fluid" style="direction: rtl; font-family: 'Cairo','Amiri',sans-serif;">
 
   <%
-    const pageSize = 10;
+    const pageSize = 14;
     const indexedMaterials = (materials || []).map((m, idx) => ({ material: m, rowNumber: idx + 1 }));
     const groupedAllMap = new Map();
     indexedMaterials.forEach((item) => {
@@ -79,7 +84,7 @@ const currencySymbol = (typeof defaultCurrency !== 'undefined' && defaultCurrenc
         </thead>
         <tbody>
           <% groupedMaterials.forEach((group) => { %>
-            <% group.forEach((item, idxInGroup) => { const m = item.material; const netW = parseFloat(m.packaging_weight); const unitCost = parseFloat(m.unit_cost)||0; const costPerKg = (!isNaN(netW) && netW>0) ? (unitCost/netW) : null; %>
+            <% group.forEach((item, idxInGroup) => { const m = item.material; const netW = parseFloat(m.packaging_weight); const unitCost = parseFloat(m.unit_cost)||0; const computedCostPerKg = (!isNaN(netW) && netW>0) ? (unitCost/netW) : null; const storedCostSource = (currencySymbol === 'SYP') ? m.price_before_waste_syp : m.price_before_waste; const storedCostPerKg = (storedCostSource !== null && storedCostSource !== undefined && String(storedCostSource).trim() !== '') ? storedCostSource : null; const costPerKg = storedCostPerKg !== null ? storedCostPerKg : computedCostPerKg; %>
             <tr>
               <td style="padding: 6pt 4pt; text-align:center;"><%= num(item.rowNumber,0) %></td>
               <% if (idxInGroup === 0) { %>
@@ -92,7 +97,7 @@ const currencySymbol = (typeof defaultCurrency !== 'undefined' && defaultCurrenc
               <td class="nowrap-value" style="padding: 6pt 4pt;"><%= (m.packaging_weight!=null) ? num(m.packaging_weight, 3) : '-' %> كجم</td>
               <td style="padding: 6pt 4pt;"><%= (m.pieces_per_package!=null)? Number(m.pieces_per_package): '-' %></td>
               <td class="nowrap-value" style="padding: 6pt 4pt;">
-                <%= (costPerKg!=null) ? ((currencySymbol==='SYP')? String(Math.round(costPerKg)) : costPerKg.toFixed(2)) : '-' %>
+                <%= (costPerKg!=null) ? num(costPerKg, (currencySymbol==='SYP' ? 0 : 2)) : '-' %>
                 <%= currencySymbol %>
               </td>
               <td style="padding: 6pt 4pt;"><%= fmtDate(m.created_at) %></td>
@@ -152,7 +157,7 @@ const currencySymbol = (typeof defaultCurrency !== 'undefined' && defaultCurrenc
   tfoot { display: table-footer-group; }
   tr { page-break-inside: avoid; }
   .table-responsive { overflow-x: visible; }
-  .container-fluid { padding-bottom: 80px; }
+  .container-fluid { padding-bottom: 50px; }
   .print-footer { position: fixed; left: 0; right: 0; bottom: 0; background: #fff; }
   .table-custom th, .table-custom td { padding: 4pt 3pt; }
   .page-break { page-break-after: always; }

